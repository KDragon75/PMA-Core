{
  "name": "@pma/pi-adapter",
  "version": "0.1.0",
  "license": "UNLICENSED",
  "private": true,
  "type": "module",
  "files": ["src", "dist/src", "package-lock.json"],
  "pi": { "extensions": ["./src/index.ts"] },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test dist/test/lifecycle.test.js"
  },
  "engines": { "node": ">=24 <25" },
  "dependencies": {
    "@earendil-works/pi-coding-agent": "0.80.6",
    "typebox": "1.3.6"
  },
  "devDependencies": { "typescript": "5.8.3", "@types/node": "24.0.0" }
}
