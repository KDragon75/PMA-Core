{
  "name": "@pma/node-provider",
  "version": "0.1.0",
  "license": "UNLICENSED",
  "private": true,
  "type": "module",
  "bin": { "pma-provider": "dist/src/cli.js" },
  "files": ["dist/src", "MODEL_SELECTION.md", "package-lock.json"],
  "engines": { "node": ">=24 <25" },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/src/cli.js",
    "test": "npm run build && node --test dist/test/provider.test.js",
    "test:model": "npm run build && node --test dist/test/model-smoke.test.js"
  },
  "dependencies": {
    "@huggingface/transformers": "4.2.0",
    "ajv": "8.20.0"
  }
}
