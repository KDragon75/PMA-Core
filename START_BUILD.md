# Start the PMA-Core build

Use this as the first Pi/Codex request after opening the repository:

> Read `AGENTS.md`, `PROJECT.md`, `docs/README.md`, and `tasks/001-BOOTSTRAP.md`. Then read only the design documents linked by Task 001. Create an execution plan using `PLANS.md`, verify the current repository state, and implement Task 001 only. Run every acceptance test listed in the task. Update the plan with completion evidence and stop before Task 002.

For later work, replace `001-BOOTSTRAP.md` with the next unblocked task. Keep each agent run focused on one task or one explicitly scoped execution plan.
